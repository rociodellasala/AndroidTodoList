<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include android:layout_width="match_parent" layout="@layout/empty_data"
        android:layout_height="match_parent" android:id="@+id/empty_category"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/category"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        tools:context=".features.categories.list.CategoryActivity">

    </androidx.recyclerview.widget.RecyclerView>

    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/extended_fab_add_category"
        android:contentDescription="@string/add_category"
        android:text="@string/add_category"
        app:backgroundTint="@color/theme_light_blue"
        app:icon="@drawable/ic_add"
        app:iconTint="@color/white"
        style="@style/floatingButtonStyle"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>